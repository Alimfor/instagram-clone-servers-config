server:
  port: 8000

spring:
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2KB
      max-file-size: 5MB
      max-request-size: 20MB

file:
  upload-dir: ${UPLOAD_TO_LOCAL_DIR}
  path:
    prefix: /images
